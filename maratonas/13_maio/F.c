#include <stdio.h>

int main(){
    int s, N;
    int forca_dragao[N];
    
    
    printf("forca kirito (s) e quantidade de dragoes(N) ");
    scanf("%d %d", &s, &N);

    if((s < 1 || s > 10000) && (N < 1  || N > 1000)){
        printf("nao pode ein\n");
        return -2;
    }

    for(int i = 0; i < N; i++){

    }

}
